import { createTheme } from "@mui/material/styles";

export const themaClaro = createTheme({
  palette: {
    primary: {
      main: "#aa6558",
      alternativo: "#e4c3bd",
    },
    secondary: {
      main: "#e9450d",
    },
  },
});
